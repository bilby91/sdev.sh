#!/bin/bash

case "$1" in
  bash)
    docker-compose -f docker/docker-compose.yml run --rm sdev /bin/bash
    ;;
  build)
    docker-compose -f docker/docker-compose.yml run --rm sdev yarn build
    ;;
  test)
    docker-compose -f docker/docker-compose.yml run --rm sdev yarn test
    ;;
esac
